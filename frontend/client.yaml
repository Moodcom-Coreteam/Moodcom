service: default
runtime: python27
api_version: 1
threadsafe: true

skip_files:
- ^(?!dist)  # Skip any files not in the dist folder

handlers:
- url: /
  static_files: dist/index.html
  upload: dist/index.html
- url: /
  static_dir: frontend
